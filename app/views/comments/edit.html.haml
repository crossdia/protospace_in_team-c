.container.proto-page
  .row
    .col-md-6
    %h4 Edit your comment
    = form_tag("/prototypes/#{params[:prototype_id]}/comments/#{params[:id]}", method: :patch) do
      = text_field_tag :text, @comments.text
      = submit_tag "COMMENT"
